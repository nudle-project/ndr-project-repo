alias function() [str] {conts};
{
    makefunction(item = Function.function)
    for use in XShellScript:
        function() [str] [content-of-function];
    for function_name in XShellScript:
        [] = [str];
}