#The info for Nudle Filesystem and the 7 custom filesystem commands used in the Filesystem Console, whioch is a different type of terminal than the default.

#The info for Nudle Filesystem and the 7 custom filesystem commands used in the Filesystem Console, whioch is a different type of terminal than the default.

#The Nudle Filesystem is a simple filesystem that can be used to store files and directories. It is a hierarchical filesystem, which means that it has a root directory, and each directory can contain files and other directories.

#The 7 custom filesystem commands are:
newfile
rmfile
refs
newdir
rmdir
killfile
killdir
# newfile: Creates a new file in the current directory.
# rmfile: Removes a file from the current directory.
# refs: Lists all files and directories in the current directory.
# newdir: Creates a new directory in the current directory.
# rmdir: Removes a directory from the current directory.
# killfile: Forcefully removes a file, even if it is in use.
# killdir: Forcefully removes a directory, even if it is in use.

# The Filesystem Console is a special terminal that allows you to interact with the Nudle Filesystem.
# It is a command-line interface that allows you to create, delete, and manage files and directories.
# The Filesystem Console also provides a way to view the contents of the Nudle Filesystem.

# The Nudle Filesystem is a simple filesystem that can be used to store files and directories.
# It is a hierarchical filesystem, which means that it has a root directory, and each directory can contain files and other directories.
#The usage for the killfile or killdir command is below
killdir Directory #It is used to set directories and files to read-only
killfile File